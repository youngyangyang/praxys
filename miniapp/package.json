{
  "name": "praxys-miniapp",
  "version": "0.2.0",
  "private": true,
  "description": "Praxys WeChat Mini Program (native + Skyline)",
  "scripts": {
    "sync-types": "node scripts/sync-types.cjs",
    "sync-i18n": "node scripts/sync-i18n.cjs",
    "pretypecheck": "npm run sync-types && npm run sync-i18n",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "miniprogram-api-typings": "^5.1.3",
    "typescript": "^5.6.3"
  }
}
